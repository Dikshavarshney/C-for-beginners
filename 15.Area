#include<stdio.h>
int main()
{
  float l, b;
  
  printf("Enter the length :");
  scanf("%f",&l);

  printf("Enter the breadth:");
  scanf("%f",&b);

  printf("The area of triangle is %f", (l*b)*1/25);
  return 0;
}
