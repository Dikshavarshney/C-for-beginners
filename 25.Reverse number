#include<stdio.h>
int main(){

    int i, num;

    printf("\nEnter the number from where you want to reverse: ");
    scanf("%d",&num);

    for( i = num; i > 0; i--){
        printf("\n%d",i);
    }
    return 0;
}
