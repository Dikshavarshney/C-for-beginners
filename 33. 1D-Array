#include<stdio.h>
int main(){

    int i, arr[10];
   
    for( i = 0; i < 10 ; i++){
        printf("\nEnter %d element to array",i+1);
        scanf("%d", &arr[i]);
    }

    for( i = 0; i < 10; i++ ){
        printf("\nThe %d element of an array is %d",i+1,arr[i]);
    }

    return 0;
}
